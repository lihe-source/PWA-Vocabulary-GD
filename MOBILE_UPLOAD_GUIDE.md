# iPhone 手機上傳方式

1. 在「檔案」App 下載並點選 `PWA-Vocabulary-GD-V7_2_1-FLAT.zip` 解壓縮。
2. 開啟 `https://github.com/lihe-source/PWA-Vocabulary-GD/`，確認分支為 `main`。
3. 選擇 **Add file → Upload files → choose your files**。
4. 到解壓後位置一次選取全部檔案；本包沒有需要另外建立的子資料夾。
5. Commit message 填入 `Release V7.2.1 flat package`，按 **Commit changes**。
6. 等候 GitHub Pages 部署完成，開啟 `https://lihe-source.github.io/PWA-Vocabulary-GD/`。
7. 完全關閉舊 PWA 後重新開啟，進入設定確認版本為 `V7.2.1`、資料儲存為 `IndexedDB V8`。
8. Google Drive 區塊按一次「立即同步」，再完成一項練習確認主頁天數正常。

`.nojekyll` 在 iPhone 檔案選擇器中可能不顯示；它不是本次程式更新的必要檔案，可直接略過。GitHub 上原有的隱藏檔與其他檔案不會因一般上傳而被刪除。
