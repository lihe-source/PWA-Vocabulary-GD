{
  "name": "pwa-vocabulary-gd-v7-2-2",
  "version": "7.2.2",
  "private": true,
  "type": "module",
  "description": "英文單字複習 PWA 與每日 Web Push 提醒後端",
  "scripts": {
    "check": "node --check app.js && node --check storage.js && node --check backup-schema.js && node --check study-streak.js && node --check reminder-manager.js && node --check push-config.js && node --check sw.js && node --check worker.js",
    "test": "node --test test-*.mjs",
    "worker:dev": "wrangler dev --test-scheduled",
    "worker:deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute vocabulary-reminders --remote --file=schema.sql",
    "vapid:generate": "web-push generate-vapid-keys --json"
  },
  "dependencies": {
    "web-push": "3.6.7"
  },
  "devDependencies": {
    "wrangler": "4.120.0"
  },
  "engines": {
    "node": ">=20"
  }
}
